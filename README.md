# JamalC2

> **现代化轻量级命令与控制框架**  
> 基于 Rust + Tauri 构建

---

## ✨ 特性

| 特性 | 描述 |
|------|------|
| 🔐 **端到端加密** | ChaCha20-Poly1305 认证加密 |
| 🌐 **HTTP Beacon** | 轻量级 HTTP 轮询通信 |
| 🎭 **流量伪装** | 随机 User-Agent + 伪装业务字段 |
| ⏱️ **动态心跳** | 可调节间隔 + 自动 ±20% 抖动 |
| 🖥️ **现代 GUI** | React + Tauri 跨平台界面 |
| 📦 **轻量 Implant** | ~1MB（纯 Rust 同步架构） |

---

## 🏗️ 架构

```
┌─────────────┐    HTTPS/HTTP    ┌──────────────┐
│   Implant   │ ◄──────────────► │   C2 Server  │
│  (Windows)  │   ChaCha20 加密   │  (Tauri App) │
└─────────────┘                  └──────────────┘
```

---

##  功能列表

### 已实现

| 功能 | 描述 |
|------|------|
| ✅ Shell 执行 | 远程命令执行，30秒超时 |
| ✅ 文件管理 | 浏览/上传/下载/删除 |
| ✅ 心跳调节 | 实时修改 Beacon 间隔 |
| ✅ Jitter 抖动 | >10秒自动 ±20% 随机化 |
| ✅ 断开连接 | 远程终止 Implant |
| ✅ Implant 生成 | GUI 一键生成 + 编译 |
| ✅ 流量伪装 | 伪装 Header + Body 字段 |
| ✅ 启动密钥 | `-k <key>` 验证 + 自删除 |

### 路线图

| 功能 | 优先级 |
|------|:------:|
| 屏幕截图 | P1 |
| 进程管理 | P1 |
| 键盘记录 | P2 |
| 持久化 | P2 |

---

## 📦 安装

### 方式一：一键安装（推荐）

1. **克隆项目**

```powershell
git clone https://github.com/zalehuoer/jamal.git
cd jamal
```

2. **运行安装脚本**

```powershell
.\install.bat
```

> 脚本会自动检查并安装：
> - Visual Studio Build Tools（C++ 编译工具）
> - Rust 编译器
> - Node.js
> - npm 依赖

3. **重启终端**（如果安装了新组件）

4. **再次运行脚本**（确认所有依赖已安装）

```powershell
.\install.bat
```

---

### 方式二：手动安装

#### 1. 安装 Visual Studio Build Tools

```powershell
winget install Microsoft.VisualStudio.2022.BuildTools --override "--add Microsoft.VisualStudio.Component.VC.Tools.x86.x64 --quiet --wait"
```

#### 2. 安装 Rust

```powershell
winget install Rustlang.Rustup
```

#### 3. 安装 Node.js

```powershell
winget install OpenJS.NodeJS.LTS
```

#### 4. 克隆项目并安装依赖

```powershell
git clone https://github.com/zalehuoer/jamal.git
cd jamal/server
npm install
```

#### 5. 验证安装

```powershell
rustc --version   # 应显示 rustc 1.7x.x
node --version    # 应显示 v18.x 或更高
```

---

## �🚀 快速开始

### 1. 启动 Server

```bash
cd server
npm run tauri dev
```

### 2. 创建监听器

**设置** → 输入端口 → **创建并启动**

### 3. 生成 Implant

**生成** → 选择监听器 → 填写地址 → **生成**

### 4. 部署

```powershell
.\implant.exe -k <key>
```

---

## 🔒 安全机制

### 加密

- **算法**: ChaCha20-Poly1305 (AEAD)
- **密钥**: 256-bit
- **Nonce**: 12 字节随机

### 流量伪装

**请求示例**:
```json
{
  "apiVersion": "2.1.0",
  "clientId": "a1b2c3d4-...",
  "authToken": "Bearer 8f2e4a...",
  "requestId": "req_1a2b3c...",
  "timestamp": 1703318400,
  "platform": "Windows",
  "data": "加密数据..."
}
```

**Header 伪装**:
- 随机 User-Agent (Chrome/Firefox/Windows Update)
- X-Request-ID, X-Client-Version 等

### 运行保护

- 需要 `-k <key>` 参数启动
- 参数错误自动删除自身

---

## 📁 项目结构

```
jamalC2/
├── server/           # 控制端 (Tauri + React)
├── implant/          # 被控端 (Rust)
└── shared/           # 共享库 (加密 + 消息)
```

---

## ⚙️ 技术栈

| 组件 | 技术 |
|------|------|
| Server 前端 | React + TypeScript |
| Server 后端 | Rust + Tauri + Axum |
| Implant | Rust (ureq + native-tls) |
| 加密 | chacha20poly1305 |
| 序列化 | serde + bincode |

---

## 📝 更新日志

### v1.0.0 (2024-12)

- ✅ ChaCha20-Poly1305 加密通信
- ✅ HTTP Beacon 轮询
- ✅ 流量伪装（Header + Body）
- ✅ 启动密钥 + 自删除
- ✅ 心跳调节 + Jitter 抖动
- ✅ 体积优化（1.5MB → 1MB）
- ✅ 同步架构重构（ureq 替代 reqwest）

---

## ⚠️ 免责声明

本工具**仅供安全研究和授权测试使用**。未经授权对计算机系统进行攻击属于违法行为。使用者需自行承担法律责任。
